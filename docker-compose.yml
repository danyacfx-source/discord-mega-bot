services:
  bot:
    build: .
    container_name: discord-mega-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    ports:
      - "17890:17890"